import { VideoTab } from 'features/video/components/VideoTab';
import { memo } from 'react';

export const VideoTabAutoLayout = memo(() => {
  return <VideoTab />;
});

VideoTabAutoLayout.displayName = 'VideoTabAutoLayout';
